/**
 * Nombre de ruta del modelo en el API (singular: version, client, update).
 *
 * @param {string} model_name
 * @returns {string}
 */
export function route_string(model_name) {
  return model_name
}
